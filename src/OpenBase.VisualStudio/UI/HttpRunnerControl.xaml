<UserControl x:Class="OpenBase.VisualStudio.UI.HttpRunnerControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header: Method and URL -->
        <Grid Grid.Row="0" Margin="0,0,0,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="MethodCombo" Width="80" SelectedIndex="0" Margin="0,0,5,0">
                <ComboBoxItem Content="GET"/>
                <ComboBoxItem Content="POST"/>
                <ComboBoxItem Content="PUT"/>
                <ComboBoxItem Content="DELETE"/>
                <ComboBoxItem Content="PATCH"/>
            </ComboBox>
            <TextBox x:Name="UrlText" Grid.Column="1" VerticalContentAlignment="Center" Text="https://api.example.com"/>
            <Button x:Name="SendButton" Grid.Column="2" Content="Send" Width="80" Margin="5,0,0,0" Click="SendButton_Click"/>
        </Grid>

        <!-- Request Body -->
        <GroupBox Grid.Row="1" Header="Request Body (JSON)">
            <TextBox x:Name="RequestBodyText" AcceptsReturn="True" AcceptsTab="True" FontFamily="Consolas" VerticalScrollBarVisibility="Auto"/>
        </GroupBox>

        <GridSplitter Grid.Row="2" HorizontalAlignment="Stretch" Background="Transparent"/>

        <!-- Response -->
        <GroupBox Grid.Row="3" Header="Response">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Status: " Foreground="Gray"/>
                    <TextBlock x:Name="StatusLabel" FontWeight="Bold" Margin="0,0,15,0"/>
                    <TextBlock Text="Time: " Foreground="Gray"/>
                    <TextBlock x:Name="TimeLabel" Margin="0,0,15,0"/>
                </StackPanel>
                <TextBox x:Name="ResponseBodyText" Grid.Row="1" IsReadOnly="True" AcceptsReturn="True" FontFamily="Consolas" VerticalScrollBarVisibility="Auto" Background="#f4f4f4"/>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
